# Copy this file to ".env" and fill real values. Do NOT commit your .env with secrets.

# MongoDB Atlas (SRV) cluster host, e.g. cluster0.abcde.mongodb.net
MONGO_CLUSTER=
MONGO_USER=
MONGO_PASSWORD=
MONGO_DATABASE=

# JWT secret for signing tokens (use a long random string)
JWT_SECRET=

# Optional: override Spring profile (compose already sets prod)
# SPRING_PROFILES_ACTIVE=prod
